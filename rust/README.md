# Rust

<https://www.rust-lang.org/tools/install>

## macos

Install `rust`

    ```bash
    brew install rust
    ```

Init:

    ```bash
    cargo init
    ```

## gRPC

Samples use the `tonic` crate.
It uses `tokio` as the async runtime.
